Then just repeat the the list of passed codes on a single line 
Then if it says 'make x2' (or something similar) in either the cradle details section or the additional information section in which case double all of the codes ie b55b x2 would go to b55b x4,